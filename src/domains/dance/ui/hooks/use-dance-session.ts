"use client";

/**
 * Re-export the useDanceSession hook from the context
 * This maintains backward compatibility with existing code
 */
export { useDanceSession } from "../contexts/dance-session-context";
